#include <stdio.h>

int main()
{
    float x,y;
    while(1)
    {
        scanf("%f%f", &x, &y);
        if (((x == 1 || x == 2)) || (y == 1 || y ==2))
        {
            printf("novo calculo (1-sim 2-nao)\n");
        }
        if (x == 2)
        {
            break;
        }
        else if (x == 1)
        {
            printf("%.2f",)
        }
    }
}
