Vector:
	push_back()
	find() //#include <algorithm> 
	reverse() 
	clear() // removes all the elements
	sort() //#include <algorithm> sorts accending order
 	erase() // delete a certin element
 	
List:
	int ar[5] = {1, 5, 3, 6, 4};
	list <int> mylist (ar, ar+5); // it copy's elements from the array
	push_front()
	push_back() // removes back element
	pop_front() // removes first element
	pop_back()
	find() // #include <algorithm>
	reverse()
	sort() // #include <algorithm>
	erase() 
	insert() // insert a element before iterator's location
	front() returns first element
	back() returns last element
