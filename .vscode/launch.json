{
    "version": "0.2.0",
    "configurations": [
        
        {
            "type": "vgdb",
            "request": "launch",
            "name": "C/C++ Debugs",
            "program": "${workspaceFolder}/${command:AskForProgramName}","args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}